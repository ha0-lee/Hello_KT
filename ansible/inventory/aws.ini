# Terraform의 resource "local_file" 명령으로 다 채워질 예정 주석으로 형식만 잡아두자. 어차피 상상하는 거니깐
# 1. 마스터 노드: EC2 1번
# [master]
# 여기에 Terraform이 뱉어준 aws.ini 파일의 IP 내용이 채워질 예정

# 2. 워커 노드: EC2 2번
# [worker]
# 마찬가지로 IP 채워질 예정

# [k3s_cluster:children]
# master
# worker

# [k3s_cluster:vars]
# ansible_user=ubuntu
# ansible_ssh_private_key_file=배포해준 key.pem 파일
# ansible_ssh_common_args='-o StrictHostKeyChecking=no'
